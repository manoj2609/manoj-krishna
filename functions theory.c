functions: subprograms
2. types
 1. predefined functions
 2. user defined functions
3. activities:
 1. function prototypes/declarations
 2. function defination
 3. function executuion/calling
4. function prototypes:
    function name,i\p required,(parameters/aruguments) and return type.
    syntax                   <return type> <function name>(<parameters>)
                             int add(inta intb)
5. function definations:
    implementation of the function
                             <return type> <function name>(<parameters>)
                             {
                                 <statements>
                             }
                             int add(inta,intb)
                             {
                                 int c;
                                 c=a+b;
                                 return 0;
                             }
6. function execution:
    sum=add(34,56);